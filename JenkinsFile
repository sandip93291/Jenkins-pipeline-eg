pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
                script {
                    def workspaceDir = pwd()
                    bat "javac ${workspaceDir}/Main.java"
                }
            }
        }
        stage('Run') {
            steps {
                script {
                    def workspaceDir = pwd()
                    bat "java ${workspaceDir}/Main"
                }
            }
        }
    }
}
